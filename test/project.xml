<?xml version="1.0" encoding="utf-8"?>
<project>
	
	<meta title="CatpiTest" package="" version="0.1.0" company="semmi" />
	
	<android target-sdk-version="28" />
	<!-- <meta build-number="714" /> --> <!-- let set the buildnumber manual (android) -->
	
	<!-- <window fps="60" width="800" height="600" vsync="true" antialiasing="4" resizable="true" depth-buffer="true" stencil-buffer="false" /> -->
	<window width="800" height="600" fps="61" allowHighDPI="false" />
	<window if="html5||android" width="0" height="0" />
	
	
	<!-- <app main="LimeAndOpenFLintoMAZE" path="bin" file="LimeAndOpenFLintoMAZE" /> -->
	<!-- <app main="TestView" path="bin" file="TestView" /> -->
	<!-- <app main="TestSimmulation" path="bin" file="TestSimmulation" /> -->
	<app main="FallingStones" path="bin" file="FallingStones" />

	<!-- <app main="TestCellRender" path="bin" file="TestCellRender" /> -->
	<!-- <app main="TestAutomat" path="bin" file="TestAutomat" /> -->
	<!-- <app main="TestUtil" path="bin" file="TestUtil" /> -->
	<!-- <app main="KruskalCellMergingMazeTest.Test" path="bin" file="TestKruskal" /> -->
	<!-- <app main="catpi.util.pool.TestPool" path="bin" file="TestPool" /> -->

	<!-- <app main="asset.TestAsset" path="bin" file="TestAsset" /> -->
	<!-- <haxedef name="assets" value="Actors" /> -->


	<!-- <haxedef name="CellGrid_Bytes" /> -->

	<!-- <haxedef name="catpi_macro_unroll" /> -->
	<haxedef name="viktor_safe" />
	<haxedef name="peoteview_fps" />
	
	<!-- <haxedef name="catpi_debug_view" /> -->
	
	<source path="src" />
	<source path="../src" />
	
	<haxelib name="lime" />	
	<haxelib name="peote-view" />
	<haxelib name="viktor" />
	

	<!-- force compiling for a special opengl-es version (only set one of them) -->
	<!-- this optimizes the build and forces no-runtimecheck for the gl-features -->
	<!-- <haxedef name="peoteview_es3" if="html5" /> -->
	<haxedef name="peoteview_es2" if="android" /> <!-- android autodetect es3 but did not work, so force to es2 -->
	
	<!-- use special gl-features for optimization (if supported by opengl-es version) -->
	<haxedef name="peoteview_uniformbuffers" />
	<haxedef name="peoteview_instancedrawing" />
	<haxedef name="peoteview_vertexarrayobjects" />

	<!-- <haxedef name="peoteview_debug_macro" /> -->
	<!-- <haxedef name="peoteview_debug_buffer" /> -->
	<!-- <haxedef name="peoteview_debug_view" /> -->
	<!-- <haxedef name="peoteview_debug_display" /> -->
	<!-- <haxedef name="peoteview_debug_program" /> -->
	<!-- <haxedef name="peoteview_debug_texture" /> -->
	<!-- <haxedef name="peoteview_debug_shader" /> -->

	<haxeflag name="-dce" value="full" />
	<!-- <haxedef name="webgl-debug" /> --> <!-- try with chrome and look here: https://www.khronos.org/webgl/wiki/Debugging -->
	<!-- <haxedef name="source-map-content" /> --> <!-- to debug with chrome id needs also "-debug" -->
	<!-- <haxedef name="dump=pretty output" /> -->
	<haxedef name="keep_inline_positions" />


	<!-- BUILD OPTIMIZATION by using ANALYZER and adding metas -->

	<!-- HXCPP: "@:unreflective" makes the hxcpp binary some KB smaller (only works for some packages) -->	
	<!-- <haxeflag name="--macro" value="catpi.util.BuildMacro.setMetas('util, view, render', 'unreflective')" if="windows||linux" /> -->

	<!-- HLC "@:pure" helps the analyzer to optimize more: https://haxe.org/manual/cr-static-analyzer.html
		This makes only sense for functions without side-effects (so not change global statics etc)
		-->
	<!-- <haxeflag name="--macro" value="catpi.util.BuildMacro.setMetas('asset', 'pure')" if="hlc" /> -->
	<!-- <haxedef name="analyzer-optimize" if="hlc" /> -->
	<!-- <haxedef name="analyzer-const_propagation" if="hlc" /> -->
	<!-- <haxedef name="analyzer-copy_propagation" if="hlc" /> -->
	<!-- <haxedef name="analyzer-local_dce" if="hlc" /> -->
	<!-- <haxedef name="analyzer-fusion" if="hlc" /> -->
	<!-- <haxedef name="analyzer-user-var-fusion" if="hlc" /> -->
	<!-- <haxedef name="analyzer-purity_inference" if="hlc" /> -->
	
	<assets path="assets" exclude="lime.svg|render|renderCell|renderActor|*.blend|*.sh" embed="false" />
	<icon path="assets/lime.svg" />
	
</project>