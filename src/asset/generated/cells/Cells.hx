package asset.generated.cells;

// generated by cells.blend

import asset.Sheet;
import asset.Anim;

@:publicFields enum abstract TileID(Int) from Int to Int {
    var EARTH;
    var WOOD;
    var ROCK;
    var METAL;
    var WATER;
    var AIR;
    public static var names = ["EARTH", "WOOD", "ROCK", "METAL", "WATER", "AIR"];
    @:from static public function fromString(s:String):TileID return names.indexOf(s);
    public static function iterator() return new IntIterator(0,6);
}

@:publicFields enum abstract AnimID(Int) from Int to Int {
    var still;
    public static var names = ["still"];
    @:from static public function fromString(s:String):AnimID return names.indexOf(s);
    public static function iterator() return new IntIterator(0,1);
}

// --------- Tiles ----------

interface Tile {
    public var sheet(get, never):Int;
    public var animID:Array<AnimID>;
    public function anim(id:AnimID):Anim;
}

@:publicFields class Earth implements Tile {
    inline function new () {};
    var sheet(get, never):Int; inline function get_sheet() return 0;
    inline function anim(id:AnimID):Anim {
        return switch(id) {
            case still: new Anim(0, 0);
            default: throw("Error, Earth don't have this animation"); null;
        }
    }
    var animID:Array<AnimID> = [still];
}
@:publicFields class Wood implements Tile {
    inline function new () {};
    var sheet(get, never):Int; inline function get_sheet() return 0;
    inline function anim(id:AnimID):Anim {
        return switch(id) {
            case still: new Anim(1, 1);
            default: throw("Error, Wood don't have this animation"); null;
        }
    }
    var animID:Array<AnimID> = [still];
}
@:publicFields class Rock implements Tile {
    inline function new () {};
    var sheet(get, never):Int; inline function get_sheet() return 0;
    inline function anim(id:AnimID):Anim {
        return switch(id) {
            case still: new Anim(2, 2);
            default: throw("Error, Rock don't have this animation"); null;
        }
    }
    var animID:Array<AnimID> = [still];
}
@:publicFields class Metal implements Tile {
    inline function new () {};
    var sheet(get, never):Int; inline function get_sheet() return 0;
    inline function anim(id:AnimID):Anim {
        return switch(id) {
            case still: new Anim(3, 3);
            default: throw("Error, Metal don't have this animation"); null;
        }
    }
    var animID:Array<AnimID> = [still];
}
@:publicFields class Water implements Tile {
    inline function new () {};
    var sheet(get, never):Int; inline function get_sheet() return 0;
    inline function anim(id:AnimID):Anim {
        return switch(id) {
            case still: new Anim(4, 4);
            default: throw("Error, Water don't have this animation"); null;
        }
    }
    var animID:Array<AnimID> = [still];
}
@:publicFields class Air implements Tile {
    inline function new () {};
    var sheet(get, never):Int; inline function get_sheet() return 0;
    inline function anim(id:AnimID):Anim {
        return switch(id) {
            case still: new Anim(5, 5);
            default: throw("Error, Air don't have this animation"); null;
        }
    }
    var animID:Array<AnimID> = [still];
}


class Cells {
    public static var FPS:Int = 12;
    public static var sheets:Array<Sheet> = [
        new Sheet("cells32x32.png", 32, 32, 0, 8, 1),
    ];
    public inline static function tile(id:TileID):Tile {
        return switch(id) {
            case EARTH: new Earth();
            case WOOD: new Wood();
            case ROCK: new Rock();
            case METAL: new Metal();
            case WATER: new Water();
            case AIR: new Air();
            default:  throw("Error, no tile for this ID"); null;
        }
    }
}

